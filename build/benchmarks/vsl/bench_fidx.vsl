## Index assignment benchmark: list[i] = val
define bench(n: int) -> int:
	size = 10000
	nums = range(size)
	i = 0
	while i < n:
		nums[i % size] = i * 3 + 1
		i = i + 1
	total = 0
	i = 0
	while i < size:
		total = total + nums[i]
		i = i + 1
	return total

r = bench(2000000)
print(r)
