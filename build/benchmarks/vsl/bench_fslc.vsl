## List slicing benchmark
define bench(n: int) -> int:
	src = range(1000)
	total = 0
	i = 0
	while i < n:
		start = i % 800
		s = src[start:start + 200]
		total = total + s[0]
		i = i + 1
	return total

r = bench(50000)
print(r)
