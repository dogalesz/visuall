define compute_pi(terms: int) -> float:
	pi = 0.0
	sign = 1.0
	i = 0
	while i < terms:
		pi = pi + sign / float(2 * i + 1)
		sign = sign * -1.0
		i = i + 1
	return pi * 4.0

r = compute_pi(10000000)
print(r)
