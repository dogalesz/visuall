## Dict creation benchmark
define bench(n: int) -> int:
	total = 0
	i = 0
	while i < n:
		d = {"alpha": i, "beta": i + 1, "gamma": i + 2, "delta": i + 3, "epsilon": i + 4}
		total = total + i
		i = i + 1
	return total

r = bench(200000)
print(r)
