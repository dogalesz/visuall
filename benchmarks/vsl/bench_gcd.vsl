define gcd(a: int, b: int) -> int:
	while b != 0:
		temp = b
		b = a % b
		a = temp
	return a

define gcd_sum(limit: int) -> int:
	total = 0
	i = 1
	while i <= limit:
		j = i
		while j <= limit:
			total = total + gcd(i, j)
			j = j + 1
		i = i + 1
	return total

r = gcd_sum(2000)
print(r)
