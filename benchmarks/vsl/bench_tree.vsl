define build_tree_sum(depth: int, val: int) -> int:
	if depth <= 0:
		return val
	left_sum = build_tree_sum(depth - 1, val * 2)
	right_sum = build_tree_sum(depth - 1, val * 2 + 1)
	return val + left_sum + right_sum

r = build_tree_sum(22, 1)
print(r)
