## math_helpers.vsl — Math helper module for testing the module system.

define square(x: int) -> int:
	return x * x

define cube(x: int) -> int:
	return x * x * x

define double_it(x: int) -> int:
	return x * 2

define is_even(x: int) -> bool:
	return x % 2 == 0
